#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <SDL2/SDL.h>


typedef enum bool {false, true} bool;

typedef struct point {
    int coordinates_x;
    int coordinates_y;
} point;

