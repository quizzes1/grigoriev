#include <stdio.h>

typedef struct point {
    int x;
    int y;
} point;

int main(){
    point point1;
    point1.x = 1;
    point1.y = 1;
    printf("%d %d\n",   point1.x, point1.y);
    point Point;
    printf("%d %d\n",   point1.x, point1.y);
    
}